- Make sure that you've set the proper permission to execute profiler script
- It is necessary to add tome information to Android log-file by calling "adb logcat" with "-v time" parameters.
- Pass the logfile you want to analyze as a command-line parameter to the script with -a flag for Android log-file and -u flag for Ubuntu log-file.

